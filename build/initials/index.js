// Generated by CoffeeScript 1.10.0
var Schema, arrayOf, cuid, normalize, ref;

cuid = require('cuid');

ref = require('normalizr'), normalize = ref.normalize, Schema = ref.Schema, arrayOf = ref.arrayOf;

module.exports = (function() {
  var initial, normalizer;
  initial = {
    todo: function(text) {
      return {
        id: cuid(),
        text: text,
        completed: false
      };
    }
  };
  normalizer = {
    todos: function(todos) {
      return normalizer = normalize(todos, arrayOf(new Schema('todos')));
    }
  };
  return {
    initial: initial,
    initialState: {
      todos: []
    },
    normalizer: normalizer
  };
})();
